@if(string.IsNullOrEmpty(_remainingTime))
{
    <MudProgressCircular Color="Color.Warning" Indeterminate="true" />
}
else {
    <MudText Typo=Typo.h6 Color=_color Align=Align.Center>
        @_remainingTime
    </MudText>
}
